//both numbers and strings can be stored within an array, as demonstrated by the example below.

let arr = ['dog', 1, 2, 'monkey'];
console.log(arr);

let x = (6 / 0);
let y = (10 / 0);

if (x > y) {
    console.log('Infinity can be compared')
}
else { console.log('Infinity CANNOT be compared') };
